Network is trained through 20 epochs.
Tests are carried out in a sequential way. First test code is '0', second '1', etc.
A Zero Padding layer is added at the very beginning to resize input image from 28x28 to 32x32 pixels.

